/* ── 头部：单行「标题 + 计数」，baseline 对齐、两端对齐 ──
   设计稿 4a 去掉了 ONEIRO/ARCHIVE 品牌行与 Private Dream Archive 英文眉，
   .archive-header 是页面专属的头部行，不复用 app.wxss 的 .brand/.eyebrow/.title */
.archive-header {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
}

.archive-header-title {
  color: #211d17;
  font-size: 32rpx;
  font-weight: 600;
}

/* 设计稿字面值 rgba(33,29,23,0.4) 在 --paper(#F2ECDF) 上实测约 2.4:1，
   11px 小字远达不到 4.5:1；改用已有的 --ink-70 token（同一底色下约 6.1:1）。 */
.archive-header-count {
  color: var(--ink-70);
  font-size: 22rpx;
}

.filter-banner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24rpx;
  margin-top: var(--space-tight);
  padding: 18rpx 20rpx;
  border-left: 3rpx solid rgba(184, 134, 59, 0.54);
  background: var(--paper-raised);
  color: rgba(33, 29, 23, 0.62);
  font-size: var(--fs-label);
}

.filter-clear {
  flex: 0 0 auto;
  color: var(--gold-line);
  font-size: var(--fs-label);
}

.empty {
  margin-top: var(--space-block);
  padding: 36rpx 0;
  border-top: 1rpx solid rgba(33, 29, 23, 0.12);
  border-bottom: 1rpx solid rgba(33, 29, 23, 0.12);
  color: rgba(33, 29, 23, 0.5);
  font-size: var(--fs-aux);
  line-height: 1.8;
}

/* ── 月度观察牌组 ──────────────────────────────────────────
   .month-observation-deck 是定位容器，背后两层 .deck-shadow（纸色、旋转
   偏移）垫出「一叠牌」的厚度，.month-observation 是叠在最上面那张深墨卡。
   现在 buildMonthlyCards 按月产出，卡片装进 <swiper> 可以左右翻阅——
   以前这里刻意不做翻页，是因为数据上只有当月一张，做了就是划了没反应的
   欺骗性 UI；数据补上之后这个限制不再成立。
   容器给了明确的 rpx 高度：绝对定位的 .deck-shadow 需要一个有具体高度的
   定位祖先才能用百分比撑开，swiper 本身也必须有高度才显示。卡内文案全部
   来自 dreamMemory 的模板字符串，长度有界，不会撑爆这个高度。 */
.month-observation-deck {
  position: relative;
  height: 428rpx;
  margin-top: 40rpx;
  margin-bottom: 36rpx;
}

/* swiper 必须显式给高度，且要和 .deck-shadow 的定位容器一样高，
   否则翻页时深墨卡会脱开背后那叠纸。 */
.month-observation-swiper {
  position: relative;
  z-index: 2;
  width: 100%;
  height: 100%;
  /* 投影放在 swiper 上而不是卡片上：swiper-item 会裁掉溢出内容，
     挂在卡片上的投影会被切掉。容器和卡片同尺寸，视觉等价。 */
  box-shadow: 0 28rpx 60rpx rgba(33, 29, 23, 0.28);
}

.deck-shadow {
  position: absolute;
  border: 2rpx solid rgba(33, 29, 23, 0.1);
  box-shadow: 0 12rpx 32rpx rgba(33, 29, 23, 0.1);
}

.deck-shadow-2 {
  top: 24rpx;
  left: 28rpx;
  z-index: 0;
  width: calc(100% - 28rpx);
  height: calc(100% - 24rpx);
  background: #e8dfc9;
  transform: rotate(3.5deg);
}

.deck-shadow-1 {
  top: 12rpx;
  left: 14rpx;
  z-index: 1;
  width: calc(100% - 14rpx);
  height: calc(100% - 12rpx);
  background: #ede4d0;
  transform: rotate(1.5deg);
}

.month-observation {
  position: relative;
  z-index: 2;
  box-sizing: border-box;
  display: flex;
  width: 100%;
  height: 100%;
  flex-direction: column;
  justify-content: space-between;
  padding: 40rpx;
  background: var(--ink);
  color: #f5f2e9;
}

.mo-top {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
}

/* 深卡上的对比度核算（bg #211d17，相对亮度 L≈0.0127）：
   - 设计稿字面值 rgba(184,134,59,0.85) 精确核算约 4.13:1，够不到 4.5:1
     （简报估算的约 4.8:1 有偏差）；改用不透明 --gold-line（#B8863B）,
     实测约 5.20:1，与本文件其余深卡金色标签保持一致。 */
.mo-eyebrow {
  display: block;
  color: var(--gold-line);
  font-size: 19rpx;
  letter-spacing: 6rpx;
}

/* 设计稿字面值 rgba(245,242,233,0.4) 约 3.51:1，不达标；
   提到 0.5 后约 4.71:1，通过。卡内其余次级文字统一复用这个不透明度。 */
.mo-meta {
  display: block;
  margin-top: 6rpx;
  color: rgba(245, 242, 233, 0.5);
  font-size: 21rpx;
  letter-spacing: 2rpx;
}

/* 环形徽标是纯装饰图形，按“装饰图形 ≥3:1”核算：设计稿边框用 0.5 透明度
   的旧金在深卡上只有约 2.29:1，不够；改成不透明 --gold-line（约 5.20:1）。 */
.mo-badge {
  display: flex;
  flex-shrink: 0;
  align-items: center;
  justify-content: center;
  width: 52rpx;
  height: 52rpx;
  border: 2rpx solid var(--gold-line);
  border-radius: 50%;
}

.mo-badge-dot {
  width: 10rpx;
  height: 10rpx;
  border-radius: 50%;
  background: var(--gold-line);
}

/* 纯分隔线，不承载文字信息，理论上可以豁免非文本对比度要求；
   为了和卡内其余金色元素统一，这里同样改用不透明 --gold-line。 */
.mo-divider {
  width: 44rpx;
  height: 2rpx;
  margin: 28rpx 0 24rpx;
  background: var(--gold-line);
}

/* rgba(242,236,223,0.85) 在深卡上约 10.6:1，远超 4.5:1 门槛 */
.mo-copy {
  display: block;
  flex: 1;
  color: rgba(242, 236, 223, 0.85);
  font-family: var(--font-serif);
  font-size: 34rpx;
  line-height: 1.65;
}

/* 重复线索并进本月观察卡片，当正文下面一句更淡、更小的补充——
   跟 .mo-copy 同色系但弱一档，读出「这是附注，不是并列的第二条结论」 */
.mo-recurring-note {
  display: block;
  margin-top: 16rpx;
  color: rgba(242, 236, 223, 0.6);
  font-family: var(--font-serif);
  font-size: 26rpx;
  line-height: 1.6;
}

.mo-bottom {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}

/* 设计稿字面值 rgba(245,242,233,0.45) 约 4.08:1，同样够不到 4.5:1；
   统一提到 0.5（约 4.71:1），与 .mo-meta 保持一致 */
.mo-bottom-left {
  color: rgba(245, 242, 233, 0.5);
  font-size: 21rpx;
}

/* ── 按月排列 ─────────────────────────────────────────────
   一组一个月，月内只放真的有梦的那天——不给空白天留格子，避免看起来像
   加载出错。缩略卡用 padding-top 撑出固定 3:4 竖长比例（不依赖
   aspect-ratio 属性，兼容性更稳），配合 aspectFill 避免裁切不到位；
   日期叠在图片顶部（呼应 Photos「回忆」把日期叠在缩略图上的做法，
   放顶部是为了不跟未解读占位卡底部的卡号文字打架）。 */
.calendar-list {
  margin-top: 40rpx;
}

/* ── 月份刻度轴：中心即选中 ────────────────────────────────
   最高最深的那根永远在屏幕正中，左右拖动是把不同月份「送进」中心槽。
   月份名由上面的观察卡显示，这里不重复日期牌。

   刻度定宽、彼此不留 gap（间距做在刻度自己的 padding 里），因为 JS 要靠
   「第几根 × 宽度」算滚动位置——留 gap 就得把它也算进去，多一个必须和
   CSS 保持同步的常量。宽度改动时同步改 index.js 的 TICK_PITCH_RPX。 */
.month-scrubber {
  position: relative;
  margin: 0 -40rpx;
}

/* 中心槽：一块极淡的底，标出「拖到这里就是选中」。它不拦点击。 */
.month-scrubber-slot {
  position: absolute;
  top: 0;
  bottom: 0;
  left: 50%;
  width: 88rpx;
  margin-left: -44rpx;
  border-radius: 8rpx;
  background: rgba(33, 29, 23, 0.045);
  pointer-events: none;
}

.month-scrubber-track {
  position: relative;
  width: 100%;
  white-space: nowrap;
}

/* 首尾各留半屏内边距，最早/最新的那个月也能被送到正中，
   而不是顶在边上进不了中心槽。 */
.month-scrubber-inner {
  display: inline-flex;
  align-items: flex-end;
  padding: 0 50%;
}

/* 刻度高度只表示「正在看哪个月」，跟梦的数量无关——一根刻度同时编码两件事
   时，读者分不清它高是因为在看它、还是因为那个月梦多。数量交给卡片上的
   「N 个梦」去说。当前月靠 加高 + 加粗 + 变金 + 不透明 四重强调，认不错。 */
.month-tick {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-end;
  gap: 6rpx;
  /* 定宽，且宽度就是 JS 里的 TICK_PITCH_RPX。改这里要同步改那边。 */
  width: 88rpx;
  flex: 0 0 88rpx;
  box-sizing: border-box;
  height: 76rpx;
  opacity: 0.34;
  transition: opacity 0.28s ease;
}

.month-tick.is-active {
  opacity: 1;
}

.month-tick-bar {
  width: 6rpx;
  height: 18rpx;
  border-radius: 3rpx;
  background: rgba(33, 29, 23, 0.55);
  /* 高度和颜色一起过渡：切月份时刻度是「长出来」的，不是硬切。 */
  transition: height 0.28s ease, width 0.28s ease, background-color 0.28s ease;
}

.month-tick.is-active .month-tick-bar {
  width: 10rpx;
  height: 48rpx;
  background: var(--gold-ink);
}

.month-tick-label {
  color: var(--ink-70);
  font-size: 19rpx;
  font-weight: 700;
  transition: color 0.28s ease;
}

.month-tick.is-active .month-tick-label {
  color: var(--gold-ink);
}

/* 一次只渲染一个月，所以这里不再需要月份间距，只留和刻度轴的间隔。 */
.calendar-month {
  margin-top: 30rpx;
}

.calendar-month-row {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.calendar-cell {
  position: relative;
  width: calc((100% - 32rpx) / 3);
}

.calendar-card {
  position: relative;
  width: 100%;
  padding-top: 133.33%;
}

/* 一天多梦：两层旋转偏移的纸色底卡垫出「一叠牌」的厚度感，
   缩小版的 .deck-shadow，不做角标计数。 */
.calendar-deck-shadow {
  position: absolute;
  inset: 0;
  background: var(--paper-raised, #ede4d0);
  border: 1rpx solid rgba(33, 29, 23, 0.08);
}
.calendar-deck-shadow-1 { transform: rotate(-3deg) translateY(4rpx); }
.calendar-deck-shadow-2 { transform: rotate(3deg) translateY(6rpx); }

.calendar-card-thumb {
  position: absolute;
  inset: 0;
  overflow: hidden;
  background: linear-gradient(145deg, #18202a, #68737c 62%, #e4e4dc);
}

.calendar-card-thumb.theme-tide { background: linear-gradient(145deg, #102832, #356f78 62%, #d5e8df); }
.calendar-card-thumb.theme-threshold { background: linear-gradient(145deg, #1c2130, #73634a 62%, #ead9aa); }
.calendar-card-thumb.theme-shadow { background: linear-gradient(145deg, #0d0f16, #273049 62%, #a8b2c3); }
.calendar-card-thumb.theme-falling { background: linear-gradient(145deg, #191729, #555070 62%, #ead0be); }
.calendar-card-thumb.theme-archive { background: linear-gradient(145deg, #172033, #5c6573 62%, #e0d8c4); }
.calendar-card-thumb.theme-hearth { background: linear-gradient(145deg, #201819, #755243 62%, #efd2a6); }
.calendar-card-thumb.theme-moon { background: linear-gradient(145deg, #10182b, #3e517a 62%, #dfe7f1); }

.calendar-card-image {
  width: 100%;
  height: 100%;
}

.calendar-card-placeholder {
  display: flex;
  align-items: flex-end;
  width: 100%;
  height: 100%;
  box-sizing: border-box;
  padding: 8rpx;
  color: rgba(255, 255, 255, 0.72);
  font-size: 20rpx;
  font-weight: 800;
}

/* 星期几叠在图片顶部（而不是底部），避免跟未解读占位卡底部的卡号文字
   打架——两者都需要留白，图片只有一处安全区。 */
.calendar-card-scrim {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  height: 56rpx;
  background: linear-gradient(180deg, rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0));
  pointer-events: none;
}

.calendar-card-daylabel {
  position: absolute;
  left: 10rpx;
  top: 8rpx;
  color: #fff;
  font-size: 24rpx;
  font-weight: 700;
  text-shadow: 0 1rpx 4rpx rgba(0, 0, 0, 0.4);
}

.calendar-card-weekday {
  font-size: 18rpx;
  font-weight: 500;
  opacity: 0.82;
}

.calendar-card-count {
  position: absolute;
  top: 6rpx;
  right: 6rpx;
  min-width: 28rpx;
  padding: 2rpx 6rpx;
  background: rgba(20, 17, 12, 0.6);
  color: #fff;
  font-size: 18rpx;
  font-weight: 700;
  text-align: center;
}

/* ── 点某天梦卡的放大浮层 ──────────────────────────────────
   与阶段画像详情浮层同一套「背景点击关闭 + 内容区 catchtap 挡冒泡」模式。
   多梦的天用原生 <swiper> 左右滑动切换，动效之后再用 Mobbin 参考精修。 */
.calendar-expand-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 1000;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 60rpx 40rpx;
  box-sizing: border-box;
  background: rgba(20, 17, 12, 0.6);
}

.calendar-expand-panel {
  width: 100%;
}

/* 高度按内容实测量出来：图 470 + 文字块约 219 + 底部收口 84 + 上下内边距 64
   ≈ 865rpx，留 55rpx 余量给居中后的上下呼吸，不至于把内容裁掉。 */
.calendar-expand-swiper {
  width: 100%;
  height: 920rpx;
}

/* 整卡内容垂直居中：剩余空隙均分到上下两端，而不是全部堆在文字和底部
   提示之间——那一大块空白正是之前排版最难看的地方。 */
.calendar-expand-card {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 100%;
  margin: 0;
  padding: 32rpx;
  box-sizing: border-box;
  background: var(--paper);
  text-align: center;
}

.calendar-expand-card::after {
  border: 0;
}

/* 放大后的图保持 3:4 竖版（跟列表缩略卡、跟梦卡本身同一个比例），
   不再拉成一条横的——横向铺满会把竖构图的插画上下都切掉。
   外面那圈内衬边是「裱起来」的意思，也让图和纸色卡面之间有个交代。 */
.expand-stage {
  flex: 0 0 auto;
  width: 356rpx;
  padding: 10rpx;
  box-sizing: border-box;
  border: 1rpx solid rgba(184, 134, 59, 0.34);
  background: rgba(255, 255, 255, 0.44);
}

.expand-stage .archive-thumb {
  width: 100%;
  height: 448rpx;
}

.expand-copy {
  width: 100%;
  margin-top: 28rpx;
}

.expand-date {
  display: block;
  color: var(--ink-70);
  font-size: 20rpx;
  letter-spacing: 2rpx;
}

.expand-title {
  display: block;
  margin-top: 10rpx;
  color: #211d17;
  font-family: var(--font-serif);
  font-size: 34rpx;
}

/* 允许折行，最多两行——情绪基调是这张卡上信息量最大的一句，
   不该像列表行那样被 nowrap 截掉后半截。 */
.expand-weather {
  display: -webkit-box;
  overflow: hidden;
  margin-top: 12rpx;
  color: rgba(33, 29, 23, 0.72);
  font-size: 23rpx;
  line-height: 1.5;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}

.expand-repeat {
  display: inline-block;
  margin-top: 14rpx;
  padding: 4rpx 16rpx;
  border: 1rpx solid rgba(184, 134, 59, 0.36);
  border-radius: 999rpx;
  color: var(--gold-ink);
  font-size: 20rpx;
}

.expand-foot {
  width: 100%;
  margin-top: 30rpx;
  padding-top: 22rpx;
  border-top: 1rpx solid rgba(33, 29, 23, 0.1);
}

.expand-hint {
  color: var(--ink-70);
  font-size: var(--fs-label);
  font-weight: 700;
}

.calendar-expand-counter {
  display: block;
  margin-top: 20rpx;
  color: rgba(245, 242, 233, 0.72);
  font-size: var(--fs-label);
  text-align: center;
}

.archive-thumb {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
  background: linear-gradient(145deg, #18202a, #68737c 62%, #e4e4dc);
}

.archive-thumb.theme-tide { background: linear-gradient(145deg, #102832, #356f78 62%, #d5e8df); }
.archive-thumb.theme-threshold { background: linear-gradient(145deg, #1c2130, #73634a 62%, #ead9aa); }
.archive-thumb.theme-shadow { background: linear-gradient(145deg, #0d0f16, #273049 62%, #a8b2c3); }
.archive-thumb.theme-falling { background: linear-gradient(145deg, #191729, #555070 62%, #ead0be); }
.archive-thumb.theme-archive { background: linear-gradient(145deg, #172033, #5c6573 62%, #e0d8c4); }
.archive-thumb.theme-hearth { background: linear-gradient(145deg, #201819, #755243 62%, #efd2a6); }
.archive-thumb.theme-moon { background: linear-gradient(145deg, #10182b, #3e517a 62%, #dfe7f1); }

.archive-thumb-image {
  width: 100%;
  height: 100%;
  background: transparent;
}

/* 无缩略图时的兜底占位：叠在彩色主题渐变（.archive-thumb 的 artwork
   底色）之上，因此仍需浅色文字来保持可读，与页面纸色主题无关 */
.archive-thumb-placeholder {
  display: flex;
  align-items: flex-end;
  width: 100%;
  height: 100%;
  box-sizing: border-box;
  padding: 8rpx 7rpx;
  background: linear-gradient(180deg, rgba(255, 255, 255, 0.04), rgba(5, 7, 12, 0.48));
  color: rgba(255, 255, 255, 0.72);
  font-size: 22rpx; /* 原为 11rpx ≈5.5px 不可读：设计稿 11px 漏了 rpx 的 ×2 换算 */
  font-weight: 800;
  letter-spacing: 1rpx;
}
